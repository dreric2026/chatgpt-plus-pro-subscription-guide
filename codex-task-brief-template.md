# Codex 任务说明模板：功能开发、Bug 修复与代码审查

> 在线生成 Markdown 任务说明：[GPTUPCN Codex 任务说明生成器](https://gptupcn.com/tools/codex-task-brief-generator/?utm_source=github&utm_medium=repository&utm_campaign=codex_task_brief)

Codex 的结果质量，往往取决于任务说明是否明确。仅写“帮我修一下”或“优化这个页面”，模型需要猜测目标、范围与验收方式，容易产生返工。更有效的任务说明应包含：目标、背景、允许修改的范围、限制条件、验收标准和验证命令。

## 一、通用结构

一个可执行的任务说明至少回答六个问题：

1. **要完成什么**：用一句话描述最终结果。
2. **为什么要做**：说明用户场景或当前故障。
3. **允许改哪里**：列出目录、模块或文件范围。
4. **不能改什么**：明确兼容性、安全和数据约束。
5. **怎样算完成**：给出可以逐条核对的验收标准。
6. **怎样验证**：列出测试、构建或人工检查命令。

## 二、功能开发模板

```markdown
# 任务：新增 [功能名称]

## 目标
为 [用户/角色] 提供 [能力]，最终可以 [可观察结果]。

## 背景
当前流程是：
- [现状 1]
- [现状 2]

## 修改范围
- [目录或文件 1]
- [目录或文件 2]

## 约束
- 保持现有接口向后兼容
- 不修改无关模块
- 不提交密钥、Token 或用户隐私数据
- 优先复用项目已有组件和设计规范

## 验收标准
- [ ] 用户可以完成核心流程
- [ ] 错误状态有明确提示
- [ ] 移动端和桌面端均可使用
- [ ] 现有测试继续通过
- [ ] 新逻辑有对应测试

## 验证
- 安装：`pnpm install`
- 测试：`pnpm test`
- 构建：`pnpm build`
```

## 三、Bug 修复模板

```markdown
# 任务：修复 [问题名称]

## 复现步骤
1. 打开 [页面/功能]
2. 执行 [操作]
3. 观察到 [错误结果]

## 期望结果
应该显示或返回：[正确结果]

## 已知信息
- 错误日志：[粘贴关键日志]
- 影响环境：[生产/测试/本地]
- 首次出现时间：[时间或版本]

## 修改约束
- 先定位根因，再进行最小范围修复
- 不通过隐藏错误或删除校验绕过问题
- 补充能够复现该问题的回归测试

## 验收标准
- [ ] 原复现步骤不再触发问题
- [ ] 相邻流程没有回归
- [ ] 日志和错误提示准确
- [ ] 自动化测试通过
```

## 四、代码审查模板

```markdown
# 任务：审查 [分支/提交/PR]

## 审查目标
重点检查：
- 正确性与边界条件
- 安全与隐私风险
- 性能退化
- 并发、重试与幂等性
- 测试覆盖
- 可维护性

## 输出格式
按严重程度输出问题：
- P0：会造成数据丢失、安全事故或生产不可用
- P1：会导致核心功能错误
- P2：局部错误或明显维护风险
- P3：低风险改进建议

每个问题包含：
1. 文件与行号
2. 触发条件
3. 实际影响
4. 建议修复方式
```

## 五、提高成功率的细节

- **给路径，不要只给模块名**：“修改登录功能”仍然过于宽泛。更清晰的写法是：“主要检查 `app/auth/` 与 `lib/session.ts`，不要修改支付模块。”
- **验收标准必须可观察**：“代码更优雅”无法验证；“首屏 JavaScript 减少 30%，Lighthouse Performance 不低于 90”才是可核对的目标。
- **明确外部副作用**：涉及数据库迁移、发布、邮件、支付、删除数据时，应写清是否允许执行，以及失败时的回滚方式。
- **要求报告验证结果**：完成后说明改了哪些文件、运行了哪些命令、测试是否通过、还有哪些风险。

## 六、团队复用建议

将固定规则放入仓库的 `AGENTS.md`，将单次需求放在任务说明中。仓库规则可包含技术栈、代码风格、目录边界、测试命令和禁止事项；单次任务只描述本次目标与验收标准。

如果需要快速生成规范化任务说明，可使用 [在线 Codex 任务说明生成器](https://gptupcn.com/tools/codex-task-brief-generator/?utm_source=github&utm_medium=repository&utm_campaign=codex_task_brief)，选择“新增功能”“修复 Bug”或“代码审查”，补充项目范围后复制为 Markdown。
