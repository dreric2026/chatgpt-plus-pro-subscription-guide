# [GPTUPCN｜ChatGPT Plus、Pro、Codex 中文技术站](https://gptupcn.com)

> 中文 AI 工程、ChatGPT Plus、ChatGPT Pro 与 Codex 技术实践持续更新。

# AI Agent 为什么必须可中断、可恢复：状态机、幂等与检查点设计

AI Agent 一旦从“回答问题”升级为执行多步骤任务，就必须面对网络中断、工具超时、人工确认、重复提交和外部系统失败。无论使用 ChatGPT Plus、ChatGPT Pro 还是 Codex，可靠系统都不能假设任务会一次跑完。

## 1. 用显式状态机代替隐式流程

最小状态集合可以设计为：

    queued -> running -> waiting_approval
                    -> retryable_failed
                    -> completed
                    -> terminal_failed

每次转换都记录任务编号、当前步骤、输入摘要、输出摘要、重试次数和时间戳。恢复时从最后一个已提交状态继续，而不是重新执行整条链路。

## 2. 检查点保存什么

检查点应保存可恢复状态，而不是完整聊天记录：

- 已完成步骤和版本号；
- 下一步所需的最小输入；
- 外部资源标识；
- 已执行写操作的结果；
- 验证证据与错误分类；
- 安全审批状态。

Cookie、API Key、验证码和用户隐私不应写入普通日志或检查点。

## 3. 写操作必须幂等

对于创建订单、发送消息、写数据库等操作，重试前必须有幂等键：

    idempotency_key = task_id + step_id + input_hash

服务端收到相同幂等键时，应返回第一次操作的结果，而不是再次创建资源。若目标系统不支持幂等键，就先查询状态，再决定是否补偿或重试。

## 4. 错误需要分类

至少区分三类：

- 可重试：网络超时、临时限流、短暂服务不可用；
- 需人工处理：权限不足、输入歧义、高风险写操作；
- 终止失败：参数非法、资源不存在、策略明确拒绝。

重试要使用指数退避与上限，避免故障时产生请求风暴。

## 5. 人工确认是状态，不是弹窗

高风险操作进入 waiting_approval 状态，保存动作摘要、目标系统和影响范围。确认后使用同一个任务编号继续执行；拒绝则进入已取消状态。这样页面关闭或会话中断后仍能恢复。

## 6. 补偿优先于“回滚一切”

分布式系统很难真正回滚。更实际的是为关键写操作定义补偿动作：

- 创建资源 -> 标记停用或删除；
- 修改配置 -> 恢复上一版本；
- 发送通知 -> 发送更正消息；
- 扣减额度 -> 追加反向流水。

补偿动作同样需要幂等、审计和权限控制。

## 7. 可观测性清单

每次任务至少记录：

1. task_id 与 step_id；
2. 状态转换；
3. 工具调用耗时；
4. 重试原因与次数；
5. 外部资源标识；
6. 验证结果；
7. 人工确认记录。

不要只记录“成功”或“失败”。可恢复系统必须知道失败发生在哪一步、此前哪些写操作已经生效。

## 8. 与 Codex 工作流结合

使用 Codex 处理代码任务时，可以把“读取、修改、测试、交付”建模为四个步骤。修改代码后先创建检查点，再运行格式化、类型检查和测试；验证不通过则保留差异与日志，进入可重试状态。只有验证通过后，任务才进入 completed。

这种架构不依赖具体模型或套餐。ChatGPT Plus、ChatGPT Pro 和 Codex 的能力可以变化，但状态机、幂等、检查点和验证协议仍然可复用。

## 结论

可靠的 AI Agent 不是永不失败，而是失败后能够判断：从哪里继续、哪些动作不能重复、何时需要人工确认，以及如何证明最终结果正确。

更多中文 AI 工程与 Codex 实践：[访问 GPTUPCN](https://gptupcn.com)。

*本文为独立技术内容，与 OpenAI 无隶属或背书关系。*
