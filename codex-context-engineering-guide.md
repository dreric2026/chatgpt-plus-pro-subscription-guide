# Codex 上下文工程：构建最小有效 Context Pack

> 完整中文教程：[Codex 上下文工程实战](https://gptupcn.com/blog/codex-context-engineering/?utm_source=github&utm_medium=repository&utm_campaign=codex_context)

Codex 不缺“更多材料”，真正缺的是能支持工程决策的准确证据。把整个仓库、全部日志和所有历史讨论一次性塞进任务，并不一定提高成功率，反而可能让旧信息和无关内容干扰判断。

一个好的 Context Pack 应该足够小，让人能够快速核对；也要足够完整，让执行者知道目标、边界和验证方法。

## Context Pack 的五个组成部分

### 1. 仓库规则

先提供项目的稳定事实：

- AGENTS.md
- README.md
- 安装、测试和构建命令
- 目录职责
- 代码风格与兼容要求
- 禁止修改的区域

稳定规则应保存在仓库中，而不是每次重新粘贴。

### 2. 任务目标

目标必须描述可观察结果。例如：

~~~text
为订单查询接口增加游标分页。
旧客户端不传 cursor 时保持原有行为。
重复 cursor 请求必须返回稳定结果。
~~~

“优化接口”不是目标，因为它没有说明什么变化才算完成。

### 3. 最接近问题的代码入口

先给路由、组件、服务或调用方，不要先给整个目录。让 Codex 从入口搜索符号、调用链和测试，再按证据扩大读取范围。

~~~text
入口：app/api/orders/route.ts
核心逻辑：lib/orders/query.ts
现有测试：tests/orders-pagination.test.ts
~~~

### 4. 复现证据与接口契约

错误日志只保留关键部分，并删除 Token、Cookie、邮箱和用户数据。接口任务还应提供请求、响应和必须保持的不变量。

~~~text
复现：pageSize=50，连续请求第 3 页时出现重复订单
期望：相同 cursor 的结果顺序稳定，不重复、不遗漏
~~~

### 5. 验证命令

没有验证方式的上下文是不完整的。至少列出最接近改动的测试，以及类型检查或生产构建。

~~~bash
pnpm test tests/orders-pagination.test.ts
pnpm typecheck
pnpm build
~~~

## 渐进式上下文流程

推荐把调查分成三轮：

1. **定位**：读取规则、目标和入口，画出真实调用链。
2. **验证**：读取直接依赖、测试和错误证据，区分事实与假设。
3. **扩展**：只有遇到框架行为或外部接口问题时，才继续读取官方文档和更远的模块。

每轮都记录：

- 新发现的事实；
- 尚未确认的假设；
- 下一步需要的证据；
- 是否需要调整原计划。

这种方式比一次性倾倒资料更容易审查，也能减少模型根据文件名猜实现的情况。

## 常见反例

### 反例一：只给截图

截图可以说明现象，但很难替代错误文本、请求参数和相关代码路径。应同时提供可复制的日志或复现步骤。

### 反例二：粘贴完整生产日志

完整日志通常包含大量重复信息，甚至含有隐私和密钥。应先按请求标识和时间窗口过滤，再脱敏。

### 反例三：把旧讨论当作当前契约

历史聊天只能作为线索。当前代码、测试、接口定义和已批准的设计文档才是事实来源。

### 反例四：忽略工作区现有修改

开始任务前先检查 git status 和差异，明确哪些改动属于用户，避免覆盖尚未提交的工作。

## 可复制的最小模板

~~~markdown
# 目标
[最终可观察结果]

# 上下文
- 仓库规则：[路径]
- 入口文件：[路径]
- 相关测试：[路径]
- 错误证据：[简短、脱敏]

# 约束
- [兼容要求]
- [禁止修改范围]
- [安全与隐私要求]

# 验收标准
- [ ] [行为标准]
- [ ] [边界条件]
- [ ] [测试与构建]

# 验证命令
- [命令 1]
- [命令 2]
~~~

更多 Codex 工程文章与免费任务生成工具：[GPTUPCN Codex 实践栏目](https://gptupcn.com/blog/category/codex/?utm_source=github&utm_medium=repository&utm_campaign=codex_context)
