# Codex 调试与回归测试：让 AI 改代码可验证、可回滚

> **完整中文专题： [Codex 调试、失败复现与回归测试实践](https://gptupcn.com/blog/codex-debugging-regression-testing/?utm_source=github&utm_medium=repository&utm_campaign=codex_debugging)**

Codex、ChatGPT Plus 与 ChatGPT Pro 能显著缩短编码时间，但“生成了修改”不等于“问题已经解决”。可靠的 AI 编程流程需要把模糊故障转化为可复现输入，再用自动化测试证明修复有效，同时保留清晰的回滚路径。

## 一、先建立最小失败样本

调试的第一步不是让模型直接重写整个模块，而是确定以下事实：

1. 哪个输入会稳定触发问题；
2. 实际结果与期望结果分别是什么；
3. 问题出现在哪个版本、操作系统和运行时；
4. 是否依赖数据库、缓存、网络、时区或第三方接口；
5. 最短复现命令是什么。

建议把这些信息整理成一个任务简报。示例：

~~~text
目标：修复订单状态在网络重试后重复写入
复现：pnpm test order-retry.spec.ts
期望：同一幂等键只写入一次
实际：第二次请求产生重复记录
限制：不修改公开 API，不新增数据库字段
验收：现有测试通过，并新增重复请求回归测试
~~~

范围越清楚，Codex 越容易给出小而准确的修改。

## 二、先写失败测试，再改实现

一个高质量回归测试应当在修复前失败、修复后通过，并且只验证一个核心行为。测试名称要表达业务规则，而不是实现细节。

~~~ts
it("reuses the first result when the idempotency key is repeated", async () => {
  const first = await submitOrder({ key: "case-001" });
  const second = await submitOrder({ key: "case-001" });

  expect(second.id).toBe(first.id);
  expect(await countOrders("case-001")).toBe(1);
});
~~~

如果失败依赖时间、随机数或网络请求，应固定时钟、随机种子和接口响应。只有输入可控，回归测试才不会变成偶发失败。

## 三、让 Codex 先解释，再修改

可以要求 Codex 按以下顺序工作：

- 读取相关入口、调用链和现有测试；
- 用一句话描述根因；
- 列出计划修改的文件；
- 说明可能受影响的边界；
- 实施最小补丁；
- 运行定向测试和完整测试；
- 汇总证据、未覆盖风险与回滚方式。

这类约束能减少无关重构，也方便代码审查者判断修改是否超出任务范围。

## 四、使用分层验证

不要只运行一个测试命令。推荐从快到慢分四层验证：

1. **静态检查**：格式、类型和 lint；
2. **单元测试**：覆盖被修改函数和边界输入；
3. **集成测试**：验证数据库、队列、缓存或外部接口的协作；
4. **端到端测试**：从用户入口确认关键流程。

当完整测试套件耗时较长时，可以先执行定向测试，但提交前仍应说明完整测试是否运行，以及没有运行的原因。

## 五、审查 AI 生成补丁

审查时重点检查：

- 是否扩大了修改范围；
- 是否隐藏异常或吞掉错误；
- 是否破坏向后兼容；
- 是否把敏感信息写入日志；
- 是否新增未锁定版本的依赖；
- 是否只修补表象而没有覆盖根因；
- 新测试是否真的会在旧实现上失败。

可以使用 git diff --stat 先看范围，再逐文件查看补丁。修改过大时，应拆成独立提交，让每个提交都能单独解释和回滚。

## 六、记录可验证证据

一次可靠的 Codex 任务交付至少应包含：

- 修改了哪些文件；
- 根因是什么；
- 执行了哪些测试；
- 测试结果；
- 仍然存在的风险；
- 如何回滚。

不要使用“应该没问题”作为结论。更好的写法是：“新增 1 个回归测试，定向测试 12/12 通过，完整测试 184/184 通过，未执行生产数据库迁移。”

## 七、常见失败模式

### 一次修改太多文件

先缩小任务边界，让 Codex 只处理一个可观察行为。重构与修复应分开提交。

### 测试通过但生产仍失败

检查生产环境是否存在不同的配置、数据规模、并发量或第三方响应。把差异转化为新的测试夹具。

### 回归测试偶发失败

优先排查时间、随机数、共享状态、测试顺序和网络依赖。不要简单增加重试次数掩盖不稳定性。

### 无法确认根因

保留原始日志与复现脚本，增加最小诊断信息，再让 Codex 比较成功路径与失败路径的差异。

## 八、适合团队复用的检查清单

- [ ] 故障能够稳定复现
- [ ] 期望和实际结果明确
- [ ] 修改范围与禁止项明确
- [ ] 修复前有失败测试
- [ ] 补丁保持最小
- [ ] 静态检查与测试已运行
- [ ] 敏感信息未进入日志
- [ ] 提交可以独立回滚
- [ ] 交付说明包含证据与风险

## 延伸阅读

- [Codex 调试、失败复现与回归测试实践](https://gptupcn.com/blog/codex-debugging-regression-testing/?utm_source=github&utm_medium=repository&utm_campaign=codex_debugging)
- [Codex 中文实践栏目](https://gptupcn.com/blog/category/codex/?utm_source=github&utm_medium=repository&utm_campaign=codex_debugging)

以上流程同样适用于 ChatGPT Plus、ChatGPT Pro 辅助编程场景：把任务写清楚、把验证自动化、把结果用证据交付。
